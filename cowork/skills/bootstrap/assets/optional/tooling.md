# Tooling

One row per tool. Rules about a tool (read-only on prod, ask before deleting) belong in a scoped rule file, not here.

| Tool | Use when... | Do not use when... |
|---|---|---|
| {{TOOL}} | ...{{WHEN}} | ...{{WHEN_NOT}} |
